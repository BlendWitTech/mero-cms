model DemoSession {
  id           String   @id
  packageId    String?
  databaseName String
  status       String
  expiresAt    DateTime
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt
}
