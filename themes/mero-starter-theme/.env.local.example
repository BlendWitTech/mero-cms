# Mero CMS backend API URL (server-side)
CMS_API_URL=http://localhost:3001

# Mero CMS backend API URL (client-side, for browser fetches)
NEXT_PUBLIC_API_URL=http://localhost:3001

# Mero CMS admin panel URL (used for the "Log in" button in the header)
NEXT_PUBLIC_ADMIN_URL=http://localhost:3000
