# Backend API URL (server-side — used by Next.js server components at build & runtime)
# Point this to your deployed backend (Railway, Render, Fly.io, etc.)
CMS_API_URL="https://your-backend.railway.app"
