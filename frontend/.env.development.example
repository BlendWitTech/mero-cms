# ── Development (local machine) ──────────────────────────────────────────────
# Copy to .env.local and fill in values.

# Backend API — local NestJS server
NEXT_PUBLIC_API_URL=http://localhost:3001
